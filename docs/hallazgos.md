1. Se detectó que el superadmin no puede crear usuarios de ningún tipo, incluyendo todas las demás acciones, tenemos que corregir.
2. para poder visualizar los elementos de la papelera o incluso para poder visualizar los registros luego de estos fueran restaurados, no se actualizan de manera automática, hay que dar f5 para que se vean reflejados los cambios
3. el calenadario de clases no actualizar automáticamente, se tiene que refrescar la pestaña de navegación con f5 para que se vean los nuevos registros, otra cosa, no entiendo como funcionan los turnos y las clases, por que se tiene que registrar manualmente los turnos? no se supone que debería ser mucho más fácil de programar, si por ejemplo es alguien de planilla creo que debería ser más  interactivo el despliegue de los turnos, lo veo muy tedioso y poco práctico a la hora de que un usuario tenga que registrar manualmente turno día por día de un trabajador, las clases igualmente no le veo mucha utilidad siendo que debe poder vincularse por ejemplo con algún turno disponible o mostrar tipo advertencia si en el hroario que se intenta regsitrar no hay nadie disponible, ningún entrenador, necesito que el caso de uso sea mejor planteado a un escenario real, puedes armar escenarios tipo gherkin bdd para poder desarrollar correctamente todo esto, planteando escenarios que se den desde un gym mediano hasta uno que sea tipo franquicia, y lo que menos queremos es que se pierdan entre los registros y formularios, que sea más intuitivo y mejor consolidado y entendible del por que su utilidad
4. Las politicas de organización están perfectas, si restringen cuando se intenta guardar un registro (por ejemplo hice la prueba con un cliente, la organización exige celular y documento de identidad, no exige correo), cuando se intenta guardar el registro el formulario no da algún aviso de que el correo sea obligatorio, pero al dar guardar registro, figura un error tipo "errors: ["correo must be an email"]", se necesita que las políticas sean no solo aplicadas en el frontend, si no también a nivel de backend, es necesario de manera crítica que no solo esta política si no todas las configuraciones de una organización sean para bien gestionadas no solo a nivel frontend si no tambien en el backend, por que al no ser asi puede generar el error 500 por el cual se generó este proyecto, el cual esta causando muchos problemas.